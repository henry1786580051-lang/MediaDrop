# Changelog

## V1.1.0 - 2026-08-06

### 运行环境
- macOS 与 Windows 安装包内置的 FFmpeg 统一升级到 `8.1.2`，并在打包前检查实际版本和 MP3 编码能力。
- macOS ARM64 改用 FFmpeg 官方源码构建，并静态链接 LAME，避免依赖开发电脑上的 Homebrew 动态库。
- Windows 改用 FFmpeg 8.1 稳定分支构建，x64 与 ARM64 资源分别固定 SHA-256；上游文件发生变化时会停止构建，避免静默混入未经验证的二进制文件。
- Electron 升级到 `41.10.3`，electron-builder 升级到 `26.15.3`，构建环境统一使用 Node.js 22。
- Flask、yt-dlp 与 PyInstaller 使用固定版本，避免不同时间构建得到未经验证的依赖组合。

### 可靠性
- 限制桌面主进程保留的后台服务日志长度，避免长时间运行时日志持续占用内存。
- 配置文件的并发更新改为串行原子写入，任务列表的增删与读取增加并发保护，避免设置丢失和任务遍历异常。
- 桌面端改由操作系统原子分配后台端口，并通过受本机密钥保护的健康接口确认服务身份和就绪状态，消除端口探测竞争与 TCP 误判。
- 移除启动时重复的全局缓存清理和时间戳查询参数，保留后端明确的页面禁用缓存策略。
- 前端样式与脚本拆分为本地静态资源，移除外部字体和内联事件，内容安全策略不再允许 `unsafe-inline`。

### 工程质量
- 自动化测试增至 45 项，覆盖并发配置写入、后台健康检查、严格内容安全策略及格式完成状态隔离。
- 在真实 Electron 41 窗口中验证本地静态资源、设置交互、随机端口启动和页面重载，未发现 CSP 或脚本错误。

## V1.0.9 - 2026-07-31

### Windows 兼容性
- Windows 系统代理现在兼容单地址及按 HTTP、HTTPS、SOCKS 分协议配置，并会拒绝或替换旧版本遗留的无效代理值。
- Windows 下载文件名会避开系统保留名、控制字符和末尾句点，防止媒体下载完成后在最终保存阶段失败。
- Windows ARM64 在线更新 yt-dlp 时会选择原生 ARM64 可执行文件，不再误用 x64 资源。
- Windows 暂停下载会验证整个 yt-dlp/FFmpeg 进程树；部分暂停失败时自动回滚，避免界面与实际运行状态不一致。
- Windows 源码开发模式支持标准虚拟环境 Python 路径，并修复依赖检查重复计数和错误的安装提示。

### Cookie 与可靠性
- Windows 无法读取或解密 Chrome 浏览器 Cookie 时，公开媒体解析会自动进行一次无 Cookie 重试，并让后续下载沿用该次降级；用户配置不会被清空，`cookies.txt` 也不会被跳过。
- 登录限定媒体在浏览器 Cookie 不可用时会显示明确提示，引导用户彻底关闭浏览器后重试，或改用 Firefox / `cookies.txt`。

### 工程质量
- 自动化测试增至 43 项，新增 Windows 代理、保留文件名、ARM64 工具选择及进程树暂停回滚测试。
- 内置 yt-dlp 保持为官方最新版本 `2026.07.04`，下载资源继续执行官方 SHA-256 校验。

## V1.0.8 - 2026-07-17

### 新功能
- 新增持久化任务中心，支持历史记录、队列排序、重试、移除与应用重启后自动续传
- 批量链接改为最多 4 路并行解析，并可随时取消解析
- 新增推荐、最高画质、节省空间和兼容模式，以及封装、音质、字幕、章节、封面和元数据选项
- 新增 1 至 4 个并发下载配置、下载完成系统通知和脱敏诊断报告导出
- 新增桌面应用版本检测；发现新版时按当前平台提示对应安装包，由用户确认后手动下载

### 可靠性与安全
- 修复任务重排造成的队列死锁、异步轮询旧响应覆盖完成状态，以及取消任务后遗留 ffmpeg 子进程的问题
- 修复 Windows 下 SQLite 连接未及时关闭导致任务数据库被持续占用的问题
- 精确格式下载不再静默回退到其他清晰度或动态范围，应用更新检查会沿用应用内代理
- Release 局部重建不再删除其他平台附件，Windows ARM64 改为原生 ARM 构建链
- 任务状态使用 SQLite WAL 持久化，配置文件改为刷盘后的原子替换，未完成缓存不再在重启时误删
- yt-dlp 在线更新在替换前校验官方 SHA-256 和版本号，失败时保留当前可用文件
- Electron 启用渲染器沙箱与随机本地 API 密钥，限制页面导航和外部链接协议
- 退出时只终止 MediaDrop 自己创建的进程树，不再按端口清理其他程序
- HDR / SDR 预设使用严格的后端格式约束，不会静默回退到错误动态范围

### 工程质量
- Electron 安装包改用最小文件白名单，移除重复的 Web 源码、文档媒体和 PyInstaller 中间产物
- 后端自动化测试增至 34 项，并新增 Electron 边界检查与完整前端脚本语法检查
- GitHub Actions 在 macOS、Windows 和 Linux 上先运行测试，测试通过后才允许构建
- 发布流程自动从更新日志生成 Release 说明

## V1.0.7 - 2026-07-15

### Download Progress
- **可靠 ETA 算法**：不再直接显示 yt-dlp 的瞬时预计时间，改用真实下载字节、单调时钟、滚动中位数和非对称指数平滑计算
- **可信度分级**：采样不足时显示“正在估算”，中等可信度显示预计时间范围，稳定后再显示单一倒计时
- **网络波动识别**：连续无数据时停止显示失真的 ETA，并提示正在重新估算
- **多流进度累计**：视频流切换到音频流时累计已完成字节，避免进度直接重置为零
- **阶段状态提示**：合并音视频、转换 MP3、转换封面和通用后处理不再冒充下载倒计时
- **暂停恢复保护**：暂停时间不计入下载速度，恢复后重新建立采样可信度

### Reliability
- 使用 yt-dlp `--progress-template` 读取结构化字节进度，保留旧式输出解析作为兼容回退
- 对估算文件大小使用滚动中位数，过滤单次异常值，同时允许后续精确大小替换估值
- 状态轮询会根据单调时钟刷新停滞状态，即使 yt-dlp 暂时不再输出新行也能正确提示

### Interface
- 下载速度统一使用本地化字节单位显示
- ETA 根据时长按 5 秒、15 秒或 1 分钟粒度取整，减少无意义的逐秒跳动
- 优化进度信息布局，长时间范围和阶段提示保持对齐且不溢出

### Tests & Packaging
- 新增 11 项 ETA 回归测试，覆盖稳定网络、突发流量、突然降速、断流、暂停恢复、多流切换、未知大小和估值修正
- 自动化测试总数提升至 23 项，并通过真实 yt-dlp 本地限速下载验证结构化进度链路
- 发布构建继续固定 yt-dlp `2026.07.04`

## V1.0.6 - 2026-07-13

### New Features
- **HDR 自动识别**：区分 SDR、HDR、HDR10、HDR10+、HLG 与 Dolby Vision，可选择自动、仅 HDR 或仅 SDR
- **YouTube 登录支持**：支持浏览器 Cookie 与 `cookies.txt`，提供认证状态测试
- **yt-dlp 在线维护**：可在设置中检查并更新 yt-dlp，本版本内置 `2026.07.04`
- **下载队列**：最多同时运行 2 个任务，支持排队、暂停、继续和取消

### Interface
- **完整汉化**：主界面、设置、下载状态和常见错误提示均已中文化
- **界面重构**：主界面加宽，设置改为分组式右侧抽屉并自动保存
- **格式状态独立**：MP4、MP3、JPG 及不同画质分别记录完成状态，切换后正确显示“下载”或“重新下载”
- **HDR 筛选修复**：选择“仅 HDR”后不再混入 SDR 格式

### Reliability & Security
- **安全缓存**：下载先进入独立任务缓存，成功后再原子发布到目标目录
- **无覆盖提交**：同名和并发完成的文件自动编号，避免覆盖用户文件
- **完整清理**：失败、取消、退出和下次启动时清理孤立缓存及半成品
- **路径保护**：防止目录穿越与缓存根目录符号链接劫持
- **跨磁盘支持**：下载目录位于其他磁盘时安全复制并提交完整文件

### Bug Fixes
- 修复解析后切换 MP4、MP3、JPG 仍下载原选项的问题
- 修复 MP4 完成后切换到 JPG 仍显示“重新下载”的问题
- 修复快速取消任务时过早释放并发名额的问题
- 移除完成后的二次保存传输，避免重复写入已落盘文件

### Tests & Packaging
- 新增格式状态回归测试和 12 项缓存生命周期测试
- GitHub Actions 标签发布改为构建 Windows x64/ARM64 EXE，本地构建 macOS ARM64 DMG
- 发布构建固定 yt-dlp `2026.07.04`，并排除本地配置、缓存和测试文件

## V1.0.2

### Breaking Changes
- **桌面应用不再依赖系统 Python** — Flask 后端通过 PyInstaller 编译为独立可执行文件，用户无需安装 Python 3

### New Features
- **开箱即用** — 安装包内含 Python 运行时、yt-dlp、ffmpeg，下载后直接使用，无需额外安装任何依赖
- **代理可达性检测** — 配置的代理不可达时自动降级直连，避免下载失败

### Improvements
- 移除启动时对系统 Python / yt-dlp / ffmpeg 的依赖检查（已打包）
- 错误对话框改为提示"请重新安装"（打包模式下）
- `.gitignore` 新增构建产物目录

### Technical
- `app.py` 新增 frozen 模式路径解析（`get_base_dir` / `get_resource_dir` / `get_ytdlp_path` / `get_ffmpeg_dir`）
- `main.js` 支持检测并使用打包后的二进制（`mediadrop-server` / `yt-dlp` / `ffmpeg`）
- 新增 PyInstaller spec 文件 `app/mediadrop-server.spec`
- CI 新增 Python + PyInstaller 构建步骤，自动下载 yt-dlp 和 ffmpeg 二进制
- 用户数据（config.json、下载文件）存于 Electron userData 目录，可写且持久

## V1.0.1

### New Features
- **端口自适应** — 默认端口 8899 被占用时自动切换到下一个可用端口
- **智能代理** — 自动检测系统代理，支持手动配置，代理不可达时自动降级直连
- **代理自动检测按钮** — 一键检测当前系统代理并设置
- **标题命名** — 下载文件以视频标题命名，重复下载自动加序号

### Improvements
- 移除启动时暴力杀死占用端口进程的行为
- 设置面板新增代理配置区域
- 支持 GitHub releases/latest 链接始终指向最新版本

### Bug Fixes
- Windows ARM64 安装包文件名冲突（CI 构建产物互相覆盖）

## V1.0.0

- 初始发布版本
- 支持 YouTube、TikTok、Instagram 等 1000+ 网站下载
- MP4 / MP3 / JPG 多格式输出
- 批量下载、暂停/恢复/取消、画质选择
- macOS DMG / Windows EXE 安装包
- Web 版 + Docker 支持
